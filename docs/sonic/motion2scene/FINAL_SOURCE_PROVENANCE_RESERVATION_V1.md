# Final-source reservation against explicit acquisition provenance

September 6 design revision, before final-source generation or fitting. The four
attempted whole-JSON inventories remain failed or refused records. They do not establish
absence from all local metadata. This reservation uses a named source-provenance ledger:
all generation_report.json, carrier_registry.json and candidates.json files under the
repository, sibling Motion2Scene checkout and research-data root, plus existing
final-transfer reservation files. Enumerate hidden/untracked files too; exclude only
.git, Python environment/cache and node_modules directories. Hash and parse every
selected provenance file; refuse if any cannot be read or contains a selected source
integer or an implied generation-seed range.

Reserve **9490001–9490008** exclusively for final transfer, with all derivatives,
scenes, repeated physics and sensor traces grouped with their ancestor. IDs are absent
from the explicit ledger if this check passes; this is not a claim about unrecorded,
remote or differently named provenance. No source is generated or qualified here.
The eight assigned candidates cannot be replaced based on their later success.

Before generation, a separate manifest must bind the model, prompts, seeds, conversion,
source CSV provenance and the same 0.30 s switching qualification contract. Before
admitting a generated source, compare its canonical motion fingerprint against every
prior source/derivative CSV referenced by this ledger and every newly generated sibling.
Canonicalization removes horizontal translation and quaternion sign, then rounds to
1e-6; it is duplicate detection, not proof of statistical independence. Retain refusals
and the full genealogy. Exclude all final ancestors from generator, selector and
threshold fitting; the earlier 430xx pool remains previously inspected and excluded.

This revises the evidence scope of reservation, not the frozen generator, learner,
scene assignment or live physics. CPU-only 180 s inventory ceiling; generation and
physics counts remain zero. The ledger and reservation are written only on completion.
