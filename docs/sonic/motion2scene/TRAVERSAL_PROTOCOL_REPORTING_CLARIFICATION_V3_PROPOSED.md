# Reporting clarification V3 — proposed parent and method disclosure update

The [V3 clarification](TRAVERSAL_PROTOCOL_REPORTING_CLARIFICATION_V3_PROPOSED.json), SHA256 `1f3d8dc4b98e5b6dbbecc3c0e1a65404419710bea57e855785f792bb5b4725d6`, binds [protocol V6](TRAVERSAL_PROTOCOL_AMENDMENT_V6_PROPOSED.md), primary plan V5, and the measured-tie amendment. It remains proposed and grants no execution authority. The original `parent_v4_proposal` and earlier clarification chain remain intact; `parent_protocol_proposal` is the authoritative parent.

Every calculation-defining field is structurally identical to reporting clarification V2: 20,000 draws, seed 202609081822, PCG64 corpus-then-layout draw order, linear 95% quantiles, the same 34 directed comparisons, and the same completion and mutually successful time estimands. The complete statistical payload digest is `2c993ab6420590d3b43a608e8e7c3d8d8ae8f4f0e11188ece819e02845c7519c`.

The primary descriptive intervals jointly resample three acquired-corpus blocks and 18 layout blocks, retaining both evaluation seeds within each selected layout. Shared baselines represent 36 unique episodes, not independently repeated recordings in each corpus. Layout-only resampling remains a secondary sensitivity analysis. Unknown assigned outcomes remain in completion bounds and suppress complete-outcome comparison intervals; time differences use only mutually successful matched episodes. Any bootstrap draw with zero joint-success support yields a null time interval without redrawing. Per-corpus means and variation remain explicit, and M2/M4 share acquisition trajectories.

The new disclosure records that seven empty-bootstrap observations informed the supervision amendment and that their 8,344 actual steps will be reused exactly once within the same corpus's assigned budget. The separate 1,192-step pre-simulator reservation remains outside recorded-step totals. This changes neither the statistical estimator nor the 972 assignment identities/order. No primary or reserved model performance is asserted, and separate adoption must bind actual model and reporting evidence.
