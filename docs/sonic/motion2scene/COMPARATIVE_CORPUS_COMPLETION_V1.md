# Comparative development corpus completion and matched fitting v1

Registered September 6 while the first twenty-cell acquisition is running, before
completion/admission and before any robot-data fitting. This completes the existing
[fixed assignment](COMPARATIVE_ACQUISITION_V1.md), rather than replacing failed scenes.
It is a development study on the observed 41002 bank, not the primary 24/48/96-budget
comparison and not fresh-source transfer.

## Physics and admission

If the first slice completes and its independent capture audit passes, acquire every
remaining eligible assigned generated slot (indices 2..8) and the shared raised control.
There are 27 remaining generated scenes plus one shared background: **56 executions**.
The analytic reserved-test-neighborhood rejection remains unexecuted and charged.
No new proposals, seeds, replacement scenes, command times or generator parameters
are introduced. All inherited direct-capture, paired-history, native-scene and 200 Hz
contact checks apply. Physics seed remains 8602. The two commands remain neutral
commitment and immediate d040 request at 0.30 s, with the shared legal recovery request.

Use an explicit combined result, leaving both raw slice results immutable. A new
admission record checks every paired capture and outcome, every loaded bank against
the original neutral/d040 bank, the captured feature recomputation, ray origin and
same-index state binding. It must not mask a physical failure as missing data. A
missing comparator or failed input-binding audit blocks fitting; all measured
both-fail cases remain labeled. Group by scene and source, not by frame or optimizer
seed. Sensor inputs never contain arm/scene/source identity or future outcomes.

The first analyzer checks actual switches for legality but does not explicitly require
a return switch. Retain its registered predicate unchanged. The combined admission
also reports requested-entry execution and completed legal return separately. A reset
before the return phase is a failed encounter, not evidence that the return happened.
This distinction does not waive the contact-qualified passage criterion or alter the
recorded action labels; requests are the actions, not promises of successful recovery.

Serial execution, unchanged 7500 MiB startup floor, 375 s cell timeout, at most
56 × 375 / 3600 = **5.833334 contended GPU h**. Before starting this slice, separately
check remaining 8 h/day and 24 h/week budget from completed run records; do not add
this ceiling to an outdated budget snapshot. Stop infrastructure failure and hash
changes; retain scientific failure; no reruns or replacements. Do not launch until
the first slice and its capture audit pass. If either input audit fails, investigate
and retain that failure rather than spending the completion budget on invalid data.

## Matched development fits

The completed assignment has 12 complete pairs in each of uniform, no-contrast and
Motion2Scene, but only 11 in analytic because of its geometric test-exclusion refusal.
Do not call these equal labeled counts. For a **small development fit only**, choose
the first eight geometrically eligible assigned generated slots in slot order for
every arm and the same three background pairs. Freeze those IDs before fitting and
without inspecting physical success. This gives **11 complete labeled encounters per
arm**, each with both binary action outcomes; retain the three unused ninth generated
pairs in the archive and charge their full acquisition cost. If any selected pair is
unavailable or fails the causal-input audit, no replacement and no fit occurs.

The full assigned background quota remains 3/12. The explicit equal-count fitting
subset has 3/11 backgrounds (27.27%) in every arm; disclose this development-budget
revision instead of calling it the original 25% fit mixture. All 38 distinct scenes
are measured in total, of which three shared backgrounds are reused across methods.
Shared rows are not new independent samples when repeated in four datasets.

Fit the already frozen 214→64→32→2 ReLU outcome predictor with train-only mean/std,
masked BCE, Adam 1e-3, 1000 full-batch updates, final checkpoint, and optimizer seeds
8501..8505 for all four arms: **20 fits at one development data size**. No early
stopping or checkpoint selection by fit loss. The two outcomes are retained even when
both fail; preference for walking is applied only at action selection. No third skill,
stop action or scripted scene rule is introduced. CPU fitting ceiling 600 s.

Report training loss and complete-label counts as fit diagnostics only. They establish
neither data efficiency nor unseen-layout traversal. No reserved layout outcomes,
430xx records, final transfer ancestors or prior 0.20 s pairs enter these fits. The
next evaluation must execute the fixed policies on the independently reserved panel;
no fitting decision here is based on that panel. Primary acquisition-budget claims
remain pending full historical fitting-cost accounting and independent evaluation.

Predictions for completion: all 28 new pairs pass causal-input matching; all new
input/bank audit predicates pass. No arm-ranking prediction is tested by training
loss. Report missing returns, physics failures and rejected assignments explicitly.
