# Registered CPU audit: native outer-envelope crossing

Registered 2026-09-06 before computation. Audit all fourteen completed evaluation-bank
v2 executions with the 45 authored native outer shapes from the hash-pinned native
asset inventory: 26 capsules, one sphere and 18 enclosing mesh spheres. This is a
new, separately reported measurement; the old body-origin results remain immutable.

At each recorded 50 Hz pose, project both endpoints of each transformed capsule onto
the beam normal, subtract its radius and 0.00200001 m outward allowance, and take the
minimum across all shapes. Require that support to exceed the far beam face by 0.10 m
through sixteen consecutive upright frames (0.30 s). Recompute measured 200 Hz normal
force through this potentially later horizon. A fall, reset or non-finish remains in
the denominator. The force rule remains peak per body <=1 N, as in the original score.

Prediction: all ten original positive cells still pass and all four blind/late cells
still fail. Report every changed finish time and classification. This does not certify
unrecorded poses, mesh cooking/inflation equivalence, whole-room avoidance, or hardware.
The enclosure covers authored mesh vertices and their convex hull under the recorded
rigid poses; it is a conditional native outer-envelope check, not exact cooked-mesh CCD.

Check every asset layer hash before and after the audit. Freeze script, helper,
source result and geometry hashes in a new registration before computation. CPU only;
five-minute ceiling; no physics or new scientific rollout. If some enclosures never
cross, report the stricter gate as failed rather than shrinking shapes.
