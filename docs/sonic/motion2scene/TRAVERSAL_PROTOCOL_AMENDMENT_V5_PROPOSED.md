# Proposed V5 evaluation infrastructure recovery

[V5](TRAVERSAL_PROTOCOL_AMENDMENT_V5_PROPOSED.json) changes the primary acquisition plan reference and the 24 expected M2/M4 model, training-result and completion paths after a bootstrap scene-resolution failure. It remains **unadopted and unexecuted**. The original [V4 proposal](TRAVERSAL_PROTOCOL_AMENDMENT_V4_PROPOSED.json), primary plan/adoption and failed attempt remain immutable.

The recovered primary plan has the same 12 scientific run specifications after output-path substitution: four arms, three corpus seeds, seven bootstrap branches and four encounters per corpus. No scene, seed, option, queue order, budget rule or training label changes. No primary physical outcome existed when recovery was proposed. The failed wrapper exited before Python/Isaac; its original unknown assessment and conservative 1,192-step reservation are retained separately from zero recorded physics. The affected corpus's upper allocation is 46,488 + 1,192 = 47,680, within its 50,000-step cap. All new study assignments reserve at most 557,856 steps; including the predecessor reservation gives 559,048. That reservation is neither a teacher label nor imputed actual physics.

The evaluation retains exactly 27 policies: the 24 M2/M4 checkpoints and the same three baseline definitions. Each receives the same 18 nominal world-fixed layouts and seeds 94301/94302. All **972 assignment identities and their shuffled order are exactly preserved**, with a maximum 1,158,624 evaluation steps. Only proposed output directories and primary-model provenance paths change. The geometry lock, scoring definition, sensor interface, schedule bank, stress exclusions and optional higher-budget follow-ups remain those of V4. M2/M4 still share each corpus trajectory; only three corpus seeds per arm are independent.

This is `proposal_revision=5` with `execution_scope_revision=4`. The V4 execution contract and existing protocol, inventory and reporting wire schemas are unchanged. The existing runner can form the same assignment table and correctly rejects the unadopted proposal before preparing or executing any episode. An explicit recovery adoption must still bind the repaired launcher, collector/controller source closure, native environment and actual completed primary provenance. All 24 real models remain missing at proposal creation; a development model cannot fill a slot.

Inherited implementation hashes, issue descriptions and tuning prose in V4 are historical review snapshots. They are not assertions that those hashes are the final adopted runtime or that previously completed development checks are pending. Final adoption must pin the actual repaired runtime and the independently verified current evidence for all nine gates. The completed six-context script validation is development evidence; it supplies no reserved result. This path amendment does not adopt a script threshold or silently apply a source change.

The separate [reporting clarification V2](TRAVERSAL_PROTOCOL_REPORTING_CLARIFICATION_V2_PROPOSED.md) changes its authoritative parent to V5 and its acquisition-plan reference to the recovery plan. Every calculation-defining field is identical to clarification V1: 20,000 paired crossed corpus/layout draws, seed 202609081822, all 34 comparisons, shared baselines, unknown bounds and mutually successful time contrasts.

Exact artifacts:

- V5 proposal SHA-256: `d9bc93a90389b6f1cc3c5318ae9a1d71097d2efea22108258b65425677373f20`.
- Recovery acquisition plan SHA-256: `69eba6f94bb7b30f6b3b00bf4e20f51080f2ac17a23be64b0ad08f1a7fe15e61`.
- [Creation and equality receipt](/home/linjiw/research-data/groot-wbc/m2s-evaluation-infrastructure-recovery-proposed-v5/result.json), SHA-256 `736a552dc51803fb111092541a99fe2cc5005e7ef3dd560b7a95c40c5a20e8e3`.
- [All 972 proposed assignments](/home/linjiw/research-data/groot-wbc/m2s-evaluation-v5-recovery-proposal-preview-v1/assignments.json) and [24-slot pending inventory](/home/linjiw/research-data/groot-wbc/m2s-evaluation-v5-recovery-proposal-preview-v1/inventory_template.json).
- [Retained bootstrap failure audit](/home/linjiw/research-data/groot-wbc/m2s-primary-bootstrap-resolution-failure-v1/result.json), SHA-256 `61eaac3a31112a570c5ac3ee91a8ee7126005250c21d7e6301d30584012043fb`.

Creation checked path substitution, exact assignment order, unchanged baseline definitions, world geometry and statistics. It made no clearance queries, simulator launches, reserved outcome reads or frozen-runtime edits. Remaining requirements are repaired primary adoption and acquisition, all actual checkpoint models, and a separate reserved evaluation adoption with its complete evidence inventory.

## Primary launch status update

Primary recovery adoption V2 was issued on 9 September 2026, SHA-256 `f75a952aa1e3c07f4be44c25723e25f9916eeb5db67de54c9dd74543dbfd8226`. All 12 read-only configuration checks completed at 03:32:18 UTC. The first registered recovery boundary dispatched at 03:32:53 UTC; Isaac initialized and the tracker and 299-frame bank loaded. This confirms the repaired path reaches the intended runtime. It is not a completed traversal or model result.

The [immutable execution binding](/home/linjiw/research-data/groot-wbc/m2s-primary-acquisition-dispatch-recovery-proposed-v4/execution_binding.json), SHA-256 `18cd46d44eb8364833c8343865eab0106dd4e5d8d43f2e634e13a7492ea199ac`, binds the new adoption, reviewed dispatcher source and recovery registration. Acquisition is in progress; completed models and curriculum comparisons remain pending at this launch-status cutoff. All prior failure/adoption/registration records remain preserved. This status update changes no proposed JSON and does not adopt reserved V5 evaluation.
