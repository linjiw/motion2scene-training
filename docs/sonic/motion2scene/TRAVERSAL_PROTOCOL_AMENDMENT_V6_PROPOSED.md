# Reserved traversal protocol V6 — proposed method continuation

The [V6 proposal](TRAVERSAL_PROTOCOL_AMENDMENT_V6_PROPOSED.json), SHA256 `ebc6da20373abbcd87cb12b50ef5ba816ccf01681310a5ad889306e913809c27`, binds primary acquisition plan V5 (`aacce6a9…`) and the [measured-tie supervision amendment](MEASURED_TIE_SUPERVISION_AMENDMENT_PROPOSED_V1.md) (`99bf0173…`). It does not adopt or launch the reserved evaluation and supplies no trained model.

Seven original empty-bootstrap executions in `seed93203_analytic_contrast` exposed a phase with complete, successful, exactly tied legal-action values. The original fit rejected the lack of consequential supervision. The proposed common opt-in allows such a table to initialize an exactly zero measured-regret head only when that phase has no consequential rows. Missing, incomplete, all-fail, single-action, and zero-weight support remain insufficient. Existing consequential fits and the previously selected ridge penalty of 10 remain unchanged. The normalization floor of 0.05 corrects inherited prose; it is existing solver behavior.

This is an explicit method amendment informed by those empty-bootstrap observations, alongside new controller/model paths. It is not a solely infrastructural recovery or wholly uninspected acquisition. No primary contrast encounters, student executions, primary fitted-model outcomes, or reserved outcomes informed it. The failed fit and separate CPU validation remain preserved; the validation model cannot fill a primary model slot.

The same seven original records are reused once as acquisition round 0 in their own corpus, retaining their original collection path. Their 8,344 recorded physics steps remain inside that corpus's 46,488 assigned-step cap, leaving 38,144 fresh assigned steps. Repeated training on already acquired rows does not count as new acquisition. The earlier pre-simulator attempt retains a separate 1,192-step conservative reservation, without being imputed as recorded physics. The complete study remains 557,856 assigned study steps and 559,048 including that reservation.

All 27 policy identities, 972 assignment identities and their order, 18 nominal layouts, two evaluation seeds, 24 M2/M4 slots, sensors, schedules, scoring, and baseline definitions are unchanged. Expected model and completion paths move to `m2s-primary-acquisition-execution-tie-v5`. Historical implementation gaps and provisional baseline/learner prose are explicitly identified as historical snapshots. Separate adoption must bind current source, native assets, learner, baseline evidence, all actual model slots, and all nine unchanged reserved gates.

The [reporting clarification V3](TRAVERSAL_PROTOCOL_REPORTING_CLARIFICATION_V3_PROPOSED.md) preserves the previously specified statistical calculation exactly. Its authoritative parent is V6; earlier V5, V4, and reporting versions remain linked and unchanged.

The exclusive-write builder and creation receipt are in `/home/linjiw/research-data/groot-wbc/m2s-evaluation-method-continuation-proposed-v6`. The existing reserved runner's metadata-only `preview` independently accepted V6 in `m2s-evaluation-v6-runner-preview-validation-v1`. No physics, clearance search, reserved outcome inspection, or production-source edit was performed.
