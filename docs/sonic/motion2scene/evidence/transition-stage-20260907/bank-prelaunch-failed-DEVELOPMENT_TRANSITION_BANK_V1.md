# Development transition bank v1

Register before physics: sources 41001, 41002, 41003 (existing development ancestors),
physics seeds 8721/8722, walk-commit and request-d040 at 0.30 s: twelve new empty-scene
Isaac Lab executions. No final candidate, original selector or pending v1 trial is
used. The existing source 41002 is a positive development control, not new transfer.

Keep SONIC, ideal rays, transition legality, 3.3 s return and the first-episode scorer
frozen. Set a virtual crossing plane at reference-route station 0.50 in each source's
world frame. The physical beam is disabled. Qualification requires both commands
pass that plane with 0.3 s upright stability, no full-capture reset or fall, and the
d040 entry at 0.30 s and return within 3.3–3.5 s. Every assigned failure is retained.
Prediction: all three sources qualify in both seeds. Failure is a result, not an
admission failure or permission to substitute a carrier.

Admission checks direct causal features, same-index physical state and ray origin;
matched pre-decision state/action/token histories; source CSV-to-PKL arrays within
1e-7; loaded root XY against source interpolation within 1e-4 m; loaded joint angles
against the source's joint-ordered scalar interpolation within 0.002 rad; and bitwise
loaded-bank equality across all cells of a given carrier. The joint tolerance is
explicitly a bounded check, not exact loader reproduction: the deployed loader uses
float32 quaternion interpolation/conversion, with a measured 0.000984 rad difference
from scalar interpolation on the previously recorded 41002 bank. Never compare a
new carrier with 41002's bank. Report every error, including root Z separately.

The contact collector must see the disabled beam and remain synchronized at 200 Hz.
The pair can be measurement-admitted while failing switching or traversal qualification.
Do not convert a denied switch into an executed d040 label. Bank qualification requires
legal actual entry/return, so no denied pair can supply the next construction study.

Hash-pin the source CSVs, PKLs, conversion records, scene, dependency closure and this
protocol before launch. Serial physics, inherited 7500 MiB memory floor, 375 s cell
timeout; at most 12 cells / 1.25 reserved contended GPU h. Fresh rolling 8 h/day and
24 h/week budget check. No automatic retry of a started or failed cell.

For the next construction experiment, require both seeds qualify; give analytic and
learned methods the same achieved transitions, and charge all twelve acquisitions.
Only seed 8721 supplies proposals; seed 8722 is a predeclared execution repeat, not
an untouched test. No multi-source learning advantage follows from empty-scene success.
