# Next development decision after the robust construction pilot

**Current ICRA scope (September 7, adopted from the user's fable guidance):**
[Four arms, one 24-requested-group budget, one deterministic learner and three already
qualified development carriers](FABLE_ICRA_STATUS.md). No generator refit, five-arm
24/48/96 campaign or final-source acquisition for this submission. The frozen proposal
pass admits uniform 18/18, analytic 1/18, target-only 16/18 and Motion2Scene 0/18 generated
groups. Including six shared backgrounds gives 82 assigned physical commands; acquisition
and the fixed policy evaluation are ongoing. Unequal acquired counts prevent an
equal-24-complete-label claim. [Working manuscript](ICRA_MANUSCRIPT.md).

Earlier plans and snapshots below are historical; the current protocol is
[M2S-ICRA-v1](M2S_ICRA_V1.md). The 480 original MLP assignments remain paused.

## Historical material before the fable scope revision

The [completed pilot](TRANSITION_CONSTRUCTION_RESULT.md) gives us three qualified
empty-transition carriers, one analytic scene that separates physical command
outcomes in both seeds, and zero admitted learned scenes. Its twelve scene assignments
are complete as a screening funnel; eleven are refusals, not pending simulations.
Original v1 remains separately paused at 120/600. No existing threshold or result changes.

The proposed paper contribution is still a training-data method: achieved command
contrasts and causal observations should make scene acquisition useful for a fixed
learner. The current evidence supports source-bound switching qualification and one
analytic construction; it does not establish learned proposal value or learning utility.

## Next bounded experiment to register

1. **Declare a separate nominal task contract.** Retain 10 mm target clearance and
   10 mm walk interference at the proposal's nominal pose. Report the original
   17/113-offset results separately. This changes the geometric acceptance domain
   and must be versioned before search or physics; it cannot inherit the old
   perturbation-audit claim. Physical success retains the original <=1 N, crossing
   and first-episode stability criterion. Track endpoint error, return, fall and
   reset separately, as the current d040 endpoint-tracking rejections require.
2. **Repair the initializer, not only its evaluator.** Keep the local-event architecture
   and bounded search. Build a transition-conditioned geometric teacher on the achieved
   bank; use the same teacher information for the strong analytic generator. Freeze
   the input's temporal/route alignment before fitting: achieved 50 Hz body geometry,
   authored reference route resampled at the same times, explicit 0.30 s command.
   The present reference-conditioned initializer and its raw draws remain a baseline.
   Training on finite geometric witnesses is proposal supervision, not physical
   outcome supervision. Charge teacher queries and every fitting update.
3. **Make the development split explicit.** Use 41001/41002 for generator fitting and
   41003 for an inspected development check; all three have already informed this
   revision. Do not call 41003 untouched or final transfer. Keep the original
   independent-layout neighborhoods excluded and all final candidates unacquired.
   Choose output counts, selection, retry limits and costs before new results.
4. **Complete a small physical corpus before another selector comparison.** Use the
   same paired command collector for both analytic and learned proposals. Add related
   removed/raised scenes and a fixed blocked quota. Record actual beam visibility,
   paired-state matching and all four outcomes. Reuse existing absent controls only
   with exact state/history binding and explicit accounting; no new-example count
   for reused traces. Do not let a search success stand in for a useful physical label.
5. **Isolate the common learner change.** Compare the original normalized MLP, a
   fixed-scale MLP and the existing fixed-scale regularized linear control on the
   same newly acquired development data. Keep thresholds and transition manager fixed;
   no inference-only scale patch or method-specific safety rule. Use this development
   study to choose one shared learner before the main five-arm comparison.

The nominal finite map contains 67 candidate witnesses after the current visibility,
reservation and pre-decision filters. They are not an acquired training corpus and
must not be silently promoted to labels or to independent examples. The next proposal
training should be checked against the analytic baseline's acquisition cost. If the
learned initializer provides no useful yield or cost advantage, report that result.

## Remaining submission evidence

After the common nominal interface and learner are stable: separately fit the five
registered data arms at 24/48/96 complete-example budgets, with five optimizer seeds
per arm/budget, and charge all acquisition/fitting/rejection costs. Freeze a genuinely
new layout/source panel before final evaluation, actually acquire and qualify its
ancestors, and report source-grouped effects. The three current carriers are development
support, not a replacement for new-source evaluation. A manuscript and video should
follow measured learning utility; another empty-scene demonstration is not the next
priority. No additional skill or hardware scope is required for this comparison.
