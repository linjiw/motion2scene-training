# Registered native/temporal audit of all 384 frozen placements

Registered 2026-09-06 before computation. The preceding frame-binding check passes
all fourteen measured-qpos captures (2,786 frames); its maximum body-position error
is 1.41e-6 m and maximum orientation error is 0.000131 degree. That is empirical
frame agreement, not a universal model-equivalence proof.

Freeze all 384 nominal pattern-search outputs in the original fresh-source result:
48 eight-output jobs, eight motion ancestors, two events and three model seeds. No
replacement, height adjustment, search, fitting or method selection. 430xx ancestry
remains permanently excluded from learning. Only measurement-contract auditing is
performed on those outputs. The original acceptance results stay immutable.

Construct the same shared-origin reference poses with the original hashed MJCF.
Compare the 29-capsule proxy with authored native geometry: 45 outer shapes for target
clearance (26 capsules, one sphere, 18 enclosing mesh spheres) and the 27 native
primitives for a sufficient neutral-interference witness. An outer-sphere overlap
does not establish real mesh collision. Sample original 30 Hz body poses and the
declared linear-position/shortest-SLERP interpolant at 120 Hz. Retain both endpoints.

For each fixed nominal box, target clearance must be >=10 mm and neutral native
primitive clearance <=-10 mm. Then subtract interval translation/rotation displacement
bounds and a 1e-5 m allowance from target clearances to test the declared interpolant
between the 120 Hz samples. The allowance is a stated assumption exceeding measured
frame-binding discrepancy; it is not a universal simulator/cooking error bound.
Use capped clearance at 20 mm with conservative broad-phase filtering. Capping only
lowers positive clearance bounds. Record all queries, witnesses and timing.

Predictions: **P1** all 384 retain nominal sampled native separation at both rates;
**P2** all 384 retain the target continuous-interpolant bound and a native primitive
neutral witness; **P3** reconstructed original proxy nominal clearances reproduce
the saved audit after applying the same 20 mm positive cap, within 1e-8 m.
Report failures and unresolved bounds separately; a negative outer lower bound is
not a demonstrated native-mesh collision. Benchmark matching proxy/native queries
and disclose pose/FK/preprocessing cost separately.

Scope exclusions: no original 113-offset uncertainty-domain guarantee, no cooked-mesh
or physical continuous collision detection, no controller execution and no downstream
learning claim. The full 384 nominal denominator is retained. CPU ceiling 1,200 s;
two Torch threads; no GPU. Hash-pin inputs, asset layers and implementations before
work, verify them after, and stop on any mismatch.
