# Original-clock 41002 intermediate-level extension

Registered 2026-09-05 after the complete neutral/d055 repeatability result and before extension
physics. That selected development pair passed all three new seeds. Test the already constructed
d040 reference at the same seeds 7901, 7902 and 7903, retaining the recorded neutral and d055
executions as matched comparators. These are three additional runs, one carrier, not three
additional independent motions. Do not replace or rerun any source result.

Predictions: (1) all three d040 executions pass tracker and unchanged relative-route checks;
(2) all three retain paired behavior against their seed-matched neutral under the existing
50 mm / 60% magnitude / 30% event-IoU rules; (3) every seed has strictly ordered achieved
neutral > d040 > d055 whole-body top profiles at every station in normalized route progress
[0.45, 0.55]. Report the minimum adjacent gap, including negative values. Shape ordering is
a diagnostic distinct from fixed-world obstacle clearance.

The motion, reference, conversion provenance, source comparators, code and predictions are
hash-pinned before launch. Serial trajectory-only execution, 9000 MiB startup guard, 375-second
timeout per cell; projected ceiling 0.3125 GPU-hours. Stop on infrastructure failure, retain
scientific failures, and assess only the completed three-cell batch. No Q4 or training admission
follows: the old Q4 protocol, held-out route failure and separate obstacle-physics requirements
remain unchanged. Geometry may propose diagnostic obstacles regardless of this result, with
the failed ladder status recorded and learning eligibility false.
