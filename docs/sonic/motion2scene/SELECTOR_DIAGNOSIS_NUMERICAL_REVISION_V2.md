# CPU diagnostic numerical revision v2

The [original breakpoint protocol](SELECTOR_BREAKPOINT_STUDY_V1.md) remains unchanged.
Its first CPU analysis stopped at the registered 1e-6 probability agreement check.
A follow-up measured a largest batch-of-six error of 5.364418e-6; every original
single-example readout matches exactly. This is not a changed checkpoint or evidence
that the original deployed actions were misrecorded.

Version 2 evaluates every original and mixed feature vector with batch size one,
matching deployment. Keep all twenty checkpoints, donors, groups, thresholds,
physical-scale linear controls, 300 s CPU ceiling and scientific questions unchanged.
Preserve the original script, registration, failure log and per-model comparison.
This is a numerical analysis repair, not a preprocessing repair or new policy study.
