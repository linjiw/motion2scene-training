# One constrained Kimodo candidate: completed and rejected

One preregistered CPU inference now tests the [installed constraint interface](KIMODO_CONSTRAINT_INTERFACE_V1.md). It produces a lower posture during the specified middle interval, but **fails native joint limits and the existing entry/return guards**. It has not entered an option registry or undergone physics execution. This result does not change current traversal-performance claims.

The [registration](../../../research-data/groot-wbc/m2s-kimodo-conditioned-lowheight-development-v1/registration.json), SHA256 `36dc04a9e9191f5ba94362b271d88ef3a5968705bc8ec11a19ef77fcf91009ec`, precedes the only inference. It pins the runner/source snapshots, exact checkpoint/cache/source/constraints, seed 41002, 100 denoising steps, four CPU threads and CUDA-hidden environment. No environment, tracker or existing bank was changed. The [completion receipt](../../../research-data/groot-wbc/m2s-kimodo-conditioned-lowheight-development-v1/completed.json) retains every output and failed gate.

The candidate uses 19 full-body approach/recovery keyframes, the complete six-second neutral root path and heading, and four pelvis targets 0.085 m lower at 2.0, 2.5, 3.0 and 3.5 s. Conditions refer to the **planned** qualified-neutral reference; no achieved-state prefix is implied. The raw 34-joint model output is preserved in [NPZ](../../../research-data/groot-wbc/m2s-kimodo-conditioned-lowheight-development-v1/raw_model_motion.npz), separately from the [30 Hz, 29-hinge CSV](../../../research-data/groot-wbc/m2s-kimodo-conditioned-lowheight-development-v1/generated/conditioned.csv). Only the registered coordinate translation was restored before CSV conversion. No clipping, post-processing, blending, prefix copying, padding or retiming was applied.

The source has 180 frames; native CPU FK produces 299 loaded-reference frames ending at phase 5.96 s. **There are zero physical or sensor-capture frames.** The 30 Hz source exceeds the native right ankle pitch limit by 0.047514 rad (three frames) and waist pitch by 0.006202 rad (eight frames). Maximum interpolated 50 Hz excess is 0.040780 rad. Source and loaded sampling therefore have separate reported violations, not interchangeable counts. Full details are in the [kinematic audit](../../../research-data/groot-wbc/m2s-kimodo-conditioned-lowheight-development-v1/kinematic_audit.json).

| Reference phase | Diagnostic role | Maximum joint difference | Pelvis difference | Existing position guard |
| --- | --- | ---: | ---: | --- |
| 0.20 s | Early entry | 0.24125 rad | 4.09 mm | Fail |
| 0.30 s | Early entry | 0.23242 rad | 4.06 mm | Fail |
| 0.40 s | Registered candidate entry | 0.21025 rad | 2.49 mm | Fail |
| 5.50 s | Registered candidate recovery | 0.13222 rad | 28.98 mm | Fail |

The existing checks require at most 0.05 rad joint difference and 0.01 m reference-anchor difference. Right shoulder yaw is the maximum early difference; right wrist yaw is the maximum recovery difference. These [native-reference comparisons](../../../research-data/groot-wbc/m2s-kimodo-conditioned-lowheight-development-v1/native_guard_comparison.json) predict guard rejection; they are not executed transitions. Through the planned 0–0.50 s approach, maximum hinge difference is 0.26197 rad. Velocity differences are retained descriptively, without claiming dynamic feasibility.

During 2.0–3.5 s, native pelvis lowering relative to neutral is 20.7–95.7 mm (median 54.3 mm). Lowering of the highest recorded native body origin is 18.8–112.1 mm (median 64.3 mm). These are kinematic body-origin measurements, not a collider envelope, beam clearance or supported low-height hold. The output responds to the requested interval but does not continuously realize the requested 85 mm pelvis drop.

The [condition residuals](../../../research-data/groot-wbc/m2s-kimodo-conditioned-lowheight-development-v1/condition_residuals.json) explain why position matching is insufficient. The early full-body position keys have maximum error below 8.9 mm, while global rotation errors across all full-body keys reach 0.261 rad. Full-body rotation channels were not conditioned by the installed API. Dense smoothed-root path error reaches 49.84 mm, and recovery keyframe position errors reach 42.14 mm. Consequently, apparent proximity to full-body key positions cannot substitute for hinge-space and controller-guard qualification.

The initial native-FK audit stopped on an omitted `gear_sonic.isaac_utils` import in its copied dependency tree. Its [failed log](../../../research-data/groot-wbc/m2s-kimodo-conditioned-lowheight-development-v1/audit.log) remains. A separately registered [audit-runtime amendment](../../../research-data/groot-wbc/m2s-kimodo-conditioned-lowheight-development-v1/audit_runtime_amendment.json) added the dependency closure in a separate audit snapshot; the candidate, original generation snapshot and FK checks were unchanged. Only the audit was rerun, and all finite/conversion/frame-count checks then completed. This infrastructure correction must not be counted as a second motion sample or hidden as a physical failure.

This completed attempt establishes that the installed constraint path runs and changes a candidate's timing/pose, while preserving a concrete counterexample to exactness and executability. Any further conditioning or correction is a separately registered candidate. The present candidate remains retained and rejected; current authored options and frozen tracker remain the qualified path.
