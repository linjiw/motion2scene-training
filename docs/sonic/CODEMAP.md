# Code map

Generated by `scripts/research/generate_codemap.py` from the modules' own docstrings, so
it cannot drift from the code. Regenerate rather than edit.

179 files across `gear_sonic/dataset_generation/` and `scripts/research/`,
grouped by the stage of the pipeline they serve.

## Motion generation and qualification

*Producing reference clips and deciding which the robot's body can hold at all.*

| file | what it does |
|---|---|
| `gear_sonic/dataset_generation/behaviour_predicates.py` | Ask whether the robot did the behaviour, not merely whether it survived doing something. |
| `gear_sonic/dataset_generation/episode_semantics.py` | Turn an episode's geometry into a goal object and a task instruction. |
| `gear_sonic/dataset_generation/kimodo_motion_adapter.py` | Convert Kimodo G1 MuJoCo qpos output into SONIC motion-library entries. |
| `gear_sonic/dataset_generation/kimodo_prompt_cache.py` | Precomputed prompt embeddings, so motion generation does not need a 15 GB encoder. |
| `gear_sonic/dataset_generation/motion_prefilter.py` | Reject untrackable reference motions before spending a rollout on them. |
| `gear_sonic/dataset_generation/prompt_taxonomy.py` | A structured prompt taxonomy for building a behaviour library, not a prompt list. |
| `gear_sonic/dataset_generation/reference_gate.py` | Decide whether a generated reference deserves a rollout, before spending one. |
| `gear_sonic/dataset_generation/retarget_reference.py` | Make a generated crouch reachable, since asking for a reachable one does not work. |
| `scripts/research/analyze_kimodo_rollout_batch.py` | Summarise a batch of Kimodo+SONIC rollouts: validation, acceptance, latent parity. |
| `scripts/research/analyze_reference_motions.py` | Measure the behaviour spread of generated reference motions, before any rollout. |
| `scripts/research/build_kimodo_provenance_bundle.py` | Build a typed EpisodeRequest -> GenerationResult -> ConversionResult bundle. |
| `scripts/research/build_prompt_taxonomy.py` | Emit the prompt taxonomy as a prompts file plus an auditable coverage report. |
| `scripts/research/encode_kimodo_prompts.py` | Encode motion prompts once into a cache that generation can reuse. |
| `scripts/research/export_kimodo_dataset_batch.sh` | Export every accepted rollout in a placement batch to a LeRobot synthetic_g1 dataset. |
| `scripts/research/gate_references_for_rollout.py` | Decide which generated references deserve GPU time, before any is spent. |
| `scripts/research/generate_codemap.py` | Generate the code map from the modules themselves, so it cannot drift. |
| `scripts/research/generate_kimodo_motions.py` | Generate G1 motions from prompts, writing the 36-column qpos CSVs the corpus ingests. |
| `scripts/research/hygiene_screen_bank.py` | Run the dynamic-feasibility screen over a bank of SONIC motion clips (CPU only). |
| `scripts/research/plan_kimodo_placements.py` | Plan scene placements for Kimodo G1 reference motions and emit a rollout manifest. |
| `scripts/research/run_behaviour_library_batch.sh` | Take a directory of generated motions all the way to analysed rollouts, resumably. |
| `scripts/research/run_kimodo_placement_batch.sh` | Convert and roll out every config in a placements.json produced by |
| `scripts/research/run_kimodo_sonic_rollout.sh` | Run one SONIC physics rollout of a converted Kimodo G1 reference in a repo-owned scene. |
| `scripts/research/screen_kimodo_motions.py` | Screen generated motions for configurations the G1 cannot reach. |
| `scripts/research/screen_reference_behaviours.py` | Grade every generated reference against the behaviour its prompt asked for. |

## Scene design and construction

*Choosing what an obstacle tests and placing it to an exact margin.*

| file | what it does |
|---|---|
| `gear_sonic/dataset_generation/clutter_scene_builder.py` | Build a cluttered indoor scene *around* a known-good motion path. |
| `gear_sonic/dataset_generation/criticality_map.py` | Which body part an obstacle binds, and how hard it can be made, from an executed trajectory. |
| `gear_sonic/dataset_generation/eval_scene_builder.py` | Build rooms that can grade a policy, rather than rooms fitted to one trajectory. |
| `gear_sonic/dataset_generation/eval_scene_gate.py` | Keep policy-specific geometry out of evaluation scenes. |
| `gear_sonic/dataset_generation/route_placement.py` | Place a Kimodo reference motion where the scene can actually contain it. |
| `gear_sonic/dataset_generation/scene_asset_preflight.py` | Dependency-free integrity and geometry gate for G1 dataset scene assets. |
| `gear_sonic/dataset_generation/scene_build_sheet.py` | Turn a released scene into instructions for rebuilding it on a lab floor. |
| `gear_sonic/dataset_generation/scene_route_check.py` | Refuse to spend a rollout on a scene the robot never reaches. |
| `gear_sonic/dataset_generation/scene_route_sampler.py` | Sample walkable routes through an existing scene, to generate motion *from* geometry. |
| `scripts/research/build_clutter_scenes.py` | Generate cluttered indoor scenes around known-good Kimodo motion paths. |
| `scripts/research/build_eval_scenes.py` | Build the evaluation-scene package: rooms that may grade a policy. |
| `scripts/research/build_graded_scene.py` | Write a scene whose difficulty was chosen, and verify it before a rollout is spent. |
| `scripts/research/build_scene_build_sheet.py` | Turn released scene packages into build sheets for physically rebuilding them. |
| `scripts/research/plan_graded_families.py` | Plan a family from a chosen binding part and margin, instead of searching for one. |
| `scripts/research/sample_scene_routes.py` | Plan routes through existing scenes and emit Kimodo root constraints. |

## Adaptation operators

*Constructing an adapted motion from a nominal one, locally.*

| file | what it does |
|---|---|
| `gear_sonic/dataset_generation/adaptation_cost.py` | What a whole-body adaptation costs, so "minimal adaptation" can be measured. |
| `gear_sonic/dataset_generation/local_adaptation.py` | Bend one nominal motion locally, instead of pairing two independently generated ones. |
| `gear_sonic/dataset_generation/motion_envelope.py` | Summarise what shape a motion sweeps, so pairs can be proposed without regenerating. |
| `gear_sonic/dataset_generation/self_clearance.py` | How close the robot comes to touching itself, including when it does not. |
| `gear_sonic/dataset_generation/self_intersection.py` | Detect a reference pose the robot cannot hold, using the robot's own collision model. |
| `gear_sonic/dataset_generation/swept_volume.py` | World-frame swept volume of the G1, for clearance that reflects the actual body. |

## Physics verification and gating

*Deciding whether an episode is evidence, and of what.*

| file | what it does |
|---|---|
| `gear_sonic/dataset_generation/contact_decomposition.py` | Separate robot self-contact from robot-environment contact. |
| `gear_sonic/dataset_generation/episode_outcome.py` | Classify an episode three ways, because "not accepted" hides two different facts. |
| `gear_sonic/dataset_generation/gate_policy.py` | Gate an episode according to what its label actually is. |
| `gear_sonic/dataset_generation/latent_parity.py` | SONIC 64D latent parity between Python training/eval and the C++ deploy path. |
| `gear_sonic/dataset_generation/perception_timing.py` | Was the obstacle visible before the robot started adapting to it? |
| `gear_sonic/dataset_generation/trajectory_acceptance.py` | Reason-coded pilot acceptance gates for SONIC locomotion trajectories. |
| `gear_sonic/dataset_generation/trajectory_segments.py` | Split a capture that spans an environment reset into independent passes. |
| `gear_sonic/dataset_generation/trajectory_validation.py` | Validation for raw SONIC-in-physics trajectory artifacts. |
| `scripts/research/analyze_gate_margins.py` | Report how close the corpus actually comes to each acceptance threshold. |
| `scripts/research/audit_gate_regrade.py` | Record both verdicts for every episode, and list the ones a re-grade flipped. |
| `scripts/research/audit_wall_proximity.py` | Flag rollouts whose verdict may be about the room rather than the motion. |
| `scripts/research/check_g1_dataset_scenes.py` | Preflight the repo-owned M0 G1 dataset scene package. |
| `scripts/research/check_g1_sonic_schema.py` | Validate local G1 SONIC schema assumptions used by the research plan. |
| `scripts/research/check_latent_parity.py` | Classify a recorded SONIC 64D latent against the C++ deployment token contract. |
| `scripts/research/check_sonic_locomotion_acceptance.py` | Apply reason-coded pilot acceptance gates to a SONIC trajectory pickle. |
| `scripts/research/check_sonic_trajectory.py` | Validate a raw SONIC physics trajectory before dataset export. |
| `scripts/research/check_sonic_vla_dataset.py` | Validate a SONIC VLA LeRobot dataset before GR00T fine-tuning. |
| `scripts/research/verify_family_robustness.py` | Re-run a counterfactual family's 2x2 under small initial-state perturbations. |
| `scripts/research/verify_lace_storage.py` | Verify LACE storage capacity and locked research artifacts. |
| `scripts/research/verify_schedule_path.py` | Change B launch precondition: dry-run the schedule_dict termination path (plan §3.2). |
| `scripts/research/verify_sonic_runtime_source_lock.py` | Fail closed when a frozen SONIC runtime source file has drifted. |

## Counterfactual families

*Assembling and certifying the unit of counterfactual evidence.*

| file | what it does |
|---|---|
| `gear_sonic/dataset_generation/counterfactual_family.py` | Build scene triplets where geometry, not the prompt, decides which motion works. |
| `scripts/research/analyze_family_attribution.py` | Ask *why* each cell of a counterfactual family passed or failed. |
| `scripts/research/build_counterfactual_family.py` | Build and verify one counterfactual family end to end, in physics. |
| `scripts/research/build_family_report.py` | Render one counterfactual family as a page that can be checked by eye. |
| `scripts/research/build_matched_family.py` | Build a counterfactual family from *one* motion and its own local adaptation. |
| `scripts/research/build_window_ladder.py` | Place a ladder of shelf heights across a family's predicted window. |
| `scripts/research/mine_counterfactual_pairs.py` | Search the corpus for motion pairs that could form a counterfactual family. |
| `scripts/research/report_dataset_counts.py` | The five counts that must never be collapsed into one number. |
| `scripts/research/report_family_yield.py` | Aggregate every counterfactual family attempt into a yield table. |
| `scripts/research/report_operator_yield.py` | Operator yield over *valid* nominals, with the invalid ones named rather than dropped. |

## Dataset assembly and release

*Turning verified families into something another person can use.*

| file | what it does |
|---|---|
| `gear_sonic/dataset_generation/corpus_snapshot.py` | One immutable manifest every reported statistic must come from. |
| `gear_sonic/dataset_generation/reference_payload.py` | Turn a generated reference clip into something the predicates and envelope can read. |
| `gear_sonic/dataset_generation/runtime_manifest.py` | Machine-readable success evidence for Isaac dataset rollouts. |
| `gear_sonic/dataset_generation/schemas.py` | Versioned, content-addressed contracts for synthetic G1 dataset generation. |
| `gear_sonic/dataset_generation/trajectory_export.py` | Convert validated SONIC physics trajectories into synthetic G1 frames. |
| `scripts/research/build_corpus_review.py` | Assemble everything a person needs to review the corpus by eye, organised by behaviour. |
| `scripts/research/build_corpus_snapshot.py` | Freeze the corpus into one manifest every report must read from. |
| `scripts/research/build_dataset_release.py` | Assemble everything that exists into one browsable release folder. |
| `scripts/research/freeze_scene_first_testset.py` | Sample and freeze evaluation scenes before anything is tuned against them. |
| `scripts/research/render_release_page.py` | Render the release folder as a page, generated from its own manifest. |
| `scripts/research/sample_review_cohort.py` | Freeze a stratified 100-episode cohort for independent human review. |

## Learning and evaluation

*The selector, its controls, and the experiments that decide the claims.*

| file | what it does |
|---|---|
| `gear_sonic/dataset_generation/behaviour_diversity.py` | Measure whether the corpus covers a behaviour space or repeats one behaviour. |
| `gear_sonic/dataset_generation/compatibility_selector.py` | Choose the minimal adaptation a scene requires, by scoring scene-motion compatibility. |
| `gear_sonic/dataset_generation/gpu_capacity.py` | Wait for room on a shared GPU rather than failing rollouts against a full one. |
| `scripts/research/analyze_generation_vs_tracking.py` | Does the motion generator produce clips the tracking controller can actually execute? |
| `scripts/research/evaluate_selector.py` | Validate the minimal-adaptation selector's harness on synthetic families. |
| `scripts/research/report_behaviour_diversity.py` | Report whether the corpus covers a behaviour space or repeats one behaviour. |
| `scripts/research/report_tracking_quality.py` | How faithfully does the controller execute what the generator asked for, by behaviour? |
| `scripts/research/stage2_report.py` | Stage 2: is the adapted motion trackable, with the pair still matched? |

## Rendering and presentation

*Making the result visible to a person.*

| file | what it does |
|---|---|
| `scripts/research/build_dataset_gallery.py` | Collect a browsable video gallery and compute the corpus distribution. |
| `scripts/research/render_corpus_review_page.py` | Render the corpus review as one self-contained page for visual inspection. |
| `scripts/research/render_corpus_stats.py` | Draw the corpus as it is: what it covers, what it refuses, and where it is thin. |
| `scripts/research/render_family_views.py` | Render a counterfactual family as third-person video and stills, from recorded trajectories. |
| `scripts/research/render_multiview.sh` | Render one rollout from every available camera, one view per pass. |
| `scripts/research/render_room_camera.py` | A static observer camera in the room, rendered from recorded body poses. |
| `scripts/research/visualize_synthetic_g1_episode.py` | Render a human-inspectable summary figure for one synthetic_g1 episode. |

## Adjacent: LACE

*A separate line of work sharing this repository. Not part of SweepCF.*

| file | what it does |
|---|---|
| `scripts/research/analyze_lace_transport.py` | Build a report-only LACE cross-policy transport analysis artifact. |
| `scripts/research/build_lace_analysis_protocol.py` | Build a pre-outcome, self-hashed LACE atlas analysis protocol on CPU. |
| `scripts/research/build_lace_assignment_reference_lengths.py` | Build a full-partition, assignment-only LACE reference-length inventory. |
| `scripts/research/build_lace_assignment_schedule.py` | Build a frozen assignment-only LACE probe schedule from a self-hashed spec. |
| `scripts/research/build_lace_atlas.py` | Build a deterministic LACE atlas JSON from probe-rollout and split manifests. |
| `scripts/research/build_lace_instrument.py` | Build a source-bound LACE scientific instrument without launching a simulator. |
| `scripts/research/build_lace_intervention_plan.py` | Build a frozen, reconstruction-verified LACE RQ1 intervention plan. |
| `scripts/research/build_lace_reference_feasibility.py` | Build a canonical CPU-only LACE reference-feasibility manifest. |
| `scripts/research/build_lace_reference_lengths.py` | Build a frozen, CPU-only LACE reference-length inventory. |
| `scripts/research/build_lace_schedule.py` | Build a frozen, CPU-only LACE atlas rollout schedule from a JSON spec. |
| `scripts/research/build_lace_source_panels.py` | Build representation-blind source panels for the LACE RQ1 intervention. |
| `scripts/research/build_lace_split.py` | Build and validate a frozen LACE source-disjoint split manifest. |
| `scripts/research/collect_lace_scientific_cells.py` | Collect receipt-committed LACE cells into one scientific rollout artifact. |
| `scripts/research/lock_lace_analysis_protocol.py` | Exclusively freeze a pre-outcome LACE analysis protocol commitment. |
| `scripts/research/run_lace_atlas_probe_cell.py` | Prepare or launch one locked LACE atlas-probe schedule cell. |
| `scripts/research/run_lace_rq1_training.py` | Build, CPU-compose, or receipt one fixed-distribution LACE RQ1 run. |
| `scripts/research/run_lace_throughput.py` | Plan, launch one cell, or aggregate the LACE SONIC-Lite throughput sweep. |

## Adjacent: SONIC training, curriculum and sampler

*Controller-side training and diagnosis. SweepCF consumes the trained policy, it does not train it.*

| file | what it does |
|---|---|
| `scripts/research/aggregate_sonic_comparisons.py` | Aggregate seed-level SONIC paired-comparison artifacts. |
| `scripts/research/build_curriculum_manifest.py` | Build a CG-WBC curriculum manifest from a SONIC/VLA LeRobot dataset. |
| `scripts/research/classify_sampler_diagnosis.py` | Mechanize the preregistered SIM-M4b sampler-diagnosis classification rule. |
| `scripts/research/compare_sonic_manifests.py` | Compare SONIC experiment manifests for controlled paper experiments. |
| `scripts/research/create_tiny_sonic_vla_fixture.py` | Create a tiny schema-valid SONIC/VLA LeRobot-style fixture dataset. |
| `scripts/research/curriculum_sampler.py` | CG-WBC v0 curriculum scoring, difficulty, and sampling utilities. |
| `scripts/research/diagnose_sonic_eval_logs.py` | Summarize SONIC eval-log health for metric-completeness debugging. |
| `scripts/research/dump_sampler_checkpoint_state.py` | Dump the adaptive sampler's trained state from SONIC checkpoints. |
| `scripts/research/export_sonic_trajectory.py` | Export an aligned SONIC/PhysX trajectory and Isaac RGB video to LeRobot. |
| `scripts/research/paired_stats.py` | Small-n paired statistics for SONIC comparisons. |
| `scripts/research/plan_sonic_paired_learning_curve.py` | Plan and aggregate matched official SONIC learning-curve evaluations. |
| `scripts/research/run_sonic_eval_metric_smoke.py` | Run a bounded SONIC eval-only metric smoke from a JSON spec. |
| `scripts/research/run_sonic_multiseed.py` | Run a multi-seed paired SONIC experiment from one seed-templated spec. |
| `scripts/research/run_sonic_paired_experiment.py` | Run or plan paired SONIC experiments from an explicit paper-validation spec. |
| `scripts/research/sampler_dynamics_sim.py` | Numerical experiment: forecast adaptive-sampler dynamics without GPU. |
| `scripts/research/smoke_groot_sonic_loader.py` | Run a real UNITREE_G1_SONIC sample through NVIDIA Isaac-GR00T's loader. |
| `scripts/research/sonic_experiment_manifest.py` | Build and validate SONIC experiment manifests for paired paper comparisons. |
| `scripts/research/stall_diagnostic.py` | Stall diagnostic: capacity-limited vs curriculum-limited (plan D12). |
| `scripts/research/summarize_curriculum_eval.py` | Summarize CG-WBC stage-wise manifest/eval results. |
| `scripts/research/summarize_sampler_telemetry.py` | Summarize adaptive-sampler telemetry (Env/adp_samp/*) from SONIC training logs. |
| `scripts/research/summarize_sonic_logs.py` | Summarize SONIC IsaacLab training/eval logs into paper-friendly artifacts. |
| `scripts/research/update_curriculum_state.py` | Update CG-WBC curriculum state from stage-wise metrics. |

## Adjacent: earlier phases and other cohorts

*Superseded phases and unrelated dataset cohorts, kept for provenance.*

| file | what it does |
|---|---|
| `scripts/research/ablate_station_choice.py` | Does choosing where to put the obstacle matter? Measure it against two baselines. |
| `scripts/research/analyze_horizon_acceptance.py` | Measure how acceptance depends on how much of an episode is graded. |
| `scripts/research/benchmark_pipeline_throughput.sh` | Measure what one episode actually costs, end to end, on this machine. |
| `scripts/research/build_bones_seed_official_cohort.py` | Freeze a bounded, metadata-stratified cohort from official BONES-SEED. |
| `scripts/research/build_bones_seed_paired_manifest.py` | Validate and hash-bind a materialized official BONES-SEED SONIC cohort. |
| `scripts/research/classify_m5_activation.py` | Preregistered SIM-M5 activation-gate classifier (research_plan_zpd_teacher.md §4, v1.1). |
| `scripts/research/classify_sim_d1_headroom.py` | Classify the preregistered SIM-D1 dataset-headroom gate. |
| `scripts/research/evaluate_synthetic_g1_dataset.py` | Semantic validity checks for a synthetic_g1 VLA dataset. |
| `scripts/research/materialize_bones_seed_official_cohort.py` | Materialize a hash-locked official BONES-SEED cohort without broad extraction. |
| `scripts/research/open_loop_groot_eval.sh` | — |
| `scripts/research/run_g0_golden_path.sh` | — |
| `scripts/research/run_mvp_eval_matrix.sh` | — |
| `scripts/research/run_sim_d1_all_motion_eval.py` | Run the fail-closed SIM-D1 all-motion evaluation and classification. |
| `scripts/research/run_sim_d2_root_xy_diagnostic.py` | Run the isolated, diagnostic-only SIM-D2 root-XY evaluation. |

## Unclassified

*Not matched by any stage pattern. Listed rather than dropped: a map that silently
omits files is worse than no map.*

| file | what it does |
|---|---|
| `gear_sonic/dataset_generation/constraint_distance.py` | Per-frame metres to the binding constraint, on the executed trajectory. |
| `gear_sonic/dataset_generation/deployable_retiming.py` | Ask a crouched robot for a crouched pace, so the adapted clip can pay its own transport cost. |
| `gear_sonic/dataset_generation/motion_transitions.py` | Join two clips where the gait already agrees, because a robot never starts a crouch at frame 0. |
| `gear_sonic/dataset_generation/sweepcf_coverage.py` | Identity and coverage primitives for the SweepCF LFH index. |
| `scripts/research/analyze_hygiene_matrix.py` | Adjudicate the hygiene x sampler matched-compute matrix -- written before the matrix is run. |
| `scripts/research/batch_preflight.py` | Decide on CPU which cells are worth a GPU, and refuse to launch a batch that is not. |
| `scripts/research/build_dataset_index.py` | Flat, filterable indexes over everything the project has produced. |
| `scripts/research/build_deployable_skill.py` | Retime the adapted clips that cleared their obstacle and were rejected for arriving short. |
| `scripts/research/build_pitch_page.py` | Assemble the pitch page, embedding the matched 2x2 videos as data URIs. |
| `scripts/research/build_review_pack.py` | Render the frozen review cohort as evidence a person can judge without seeing the verdict. |
| `scripts/research/export_constraint_distance.py` | Write the per-frame distance-to-constraint record beside every rolled-out cell. |
| `scripts/research/export_decision_frames.py` | Export the ego frames a selector is allowed to see, and prove it cannot cheat. |
| `scripts/research/hygiene_repair_bank.py` | Materialise a contact-repaired copy of a SONIC motion bank, one .pkl at a time. |
| `scripts/research/materialize_test_scenes.py` | Build the frozen scene-first test set into loadable geometry. |
| `scripts/research/measure_operator_survival.py` | How much of a commanded adaptation actually appears in the executed body? |
| `scripts/research/plan_family_batch.py` | Turn screened nominals into a queue of family configurations, ranked by what they cost. |
| `scripts/research/refresh_paper_tables.py` | Regenerate the paper's measured tables in place, so no figure is ever typed by hand. |
| `scripts/research/render_difficulty_figure.py` | Show how little room there is: the shelf drops, and only the edited motion still fits. |
| `scripts/research/render_paper_page.py` | Render the working draft as a page that can actually be read. |
| `scripts/research/render_review_page.py` | Render the review cohort as a self-contained page a person can score and hand back. |
| `scripts/research/render_trajectory_view.py` | Draw the route view: where the robot went, what it passed, and where it was closest. |
| `scripts/research/run_deployable_family.py` | Roll out a deployable skill in its own family's scenes, and score the result the same way. |
| `scripts/research/score_family_batch.py` | Score banded 2x2 families: does each one show the counterfactual, and if not, why not? |
| `scripts/research/trim_clip.py` | Cut a render down to the moment it is about. |
